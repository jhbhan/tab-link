import {firestore} from './firebaseBaseService';
import { LinkModel } from "../models/Models";

export const updateLink = (newLink: LinkModel, userEmail: string) => {
  return;
}
export const deleteLink = (newLink: LinkModel, userEmail: string) => {
  return;
}